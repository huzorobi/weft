"""Weft — free-source OSINT reconnaissance aggregator.

Weft weaves many free, public open-source intelligence sources into one entity
graph: a single seed (phone, email, username, name, domain) is expanded outward
across open sources and the linked entities are drawn as a graph.

Every lookup is bound to an authorised engagement, scope-gated, and audit-logged.
See ``weft.compliance`` for the enforcement layer.
"""

__version__ = "0.4.0"
